#ifndef QUEST_HPP
    #define QUEST_HPP
#include "../Objects/object.hpp"
#include "../Objects/player.hpp"
#include <algorithm>
#include "../Util/tools.hpp"
/*
class Quest
{
	private:
	int id;
	Map *map_;
	std::vector<Object*> *objects_;
	std::vector<int> toSaveId_;
	std::vector<int> toPossess_;
	Player *player_;
	//std::vector<int> preRequise_;
	public:
	Quest(int id, Player *player);
	Quest(Player *player);
	~Quest();
	std::vector<Object*> getObjects();
	getMap()
	getPlayer();
	hasWon();
	hasLost();


};*/
#endif
